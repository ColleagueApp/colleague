name: CI
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: pnpm install eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin 
    - name: Run ESLint
      run: pnpx eslint .